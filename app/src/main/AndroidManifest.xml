<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme._05_Layout2"
        tools:targetApi="31">
        <!--
        activity: 앱의 화면 구성 즉, activity 자체가 화면 하나
            -name: 앱 실행 파일의 이름
            -exported: 다른 어플에서 이 앱을 사용할 수 있는지 없는지 여부 설정 (true: 모든 앱에서 접근 가능, false: 지정된 앱에서만 접근 가능)
        intent-filter: 특정 컴포넌트에 실행 정보를 넣어 데이터 전달
            -action: 실행할 일반적인 작업을 지정하는 문자열
            -category: 구성요소의 종류에 대한 추가 정보를 포함하는 문자열
                CATEGORY_BROWSABLE: 대상 활동이 웹브라우저에 의해 시작되어 데이터를 표시하도록 허용함 이미지 또는 이메일 메시지와 같은 링크로 참조
                CATEGORY_LAUNCHER: 작업의 최초 액티비티이며, 시스템의 애플리케이션 시작 관리자에 목록으로 게재
        -->
        <activity
            android:name=".MainActivity5"
            android:exported="true"> <!--다른 어플에서도 사용 가능 여부-->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" /> <!--실행할 화면 지정-->
                <category android:name="android.intent.category.LAUNCHER" /> <!--최초의 액티비티로 지정-->
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity4"
            android:exported="false" />
        <activity
            android:name=".MainActivity3"
            android:exported="false" />
        <activity
            android:name=".MainActivity2"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="false"/>
    </application>

</manifest>