<?xml version="1.0" encoding="utf-8"?>
<!--
[RelativeLayout 속성]
RelativeLayout: ViewGroup에서 View의 위치를 지정하고 배치하는 레이아웃
    1. 기준 View와 상대적 위치 지정
        -layout_above: 지정된 View의 위에 배치
        -layout_below: 지정된 View의 아래에 배치
        -layout_toLeftOf: 지정된 View의 왼쪽에 배치
        -layout_toRightOf: 지정된 View의 오른쪽에 배치
    2. 부모 View와 상대적 위치 지정
        -layout_alignParentLeft: true이면 부모의 왼쪽 변을 맞춤
        -layout_alignParentTop: true이면 부모의 위쪽 변을 맞춤
        -layout_alignParentRight: true이면 부모의 오른쪽 변을 맞춤
        -layout_alignParentBottom: true이면 부모의 아래쪽 변을 맞춤
        -layout_alignBaseline: 지정한 View와 베이스라인을 맞춤
        -layout_alignWithParentMissing: 기준이 발견되지 않으면 부모를 기준으로 사용
        -layout_centerHorizontal: true이면 부모의 수평 중앙에 배치
        -layout_centerVertical: true이면 부모의 수직 중앙에 배치
        -layout_centerInParent: true이면 부모의 수평, 수직 중앙에 배치
-->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".MainActivity">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginRight="10px"
        android:text="TARGET"
        android:layout_centerInParent="true"
        android:id="@+id/target"
        android:textSize="30sp"
        android:background="#00D9FF"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="DEFAULT"
        android:id="@+id/defaultView"
        android:textSize="20sp"
        android:background="#1DDB16"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toLeftOf="@id/target"
        android:text="LEFT"
        android:id="@+id/left"
        android:textSize="20sp"
        android:background="#1DDB16"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/target"
        android:text="RIGHT"
        android:id="@+id/right"
        android:textSize="20sp"
        android:background="#1DDB16" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@id/target"
        android:text="TOP"
        android:textSize="20sp"
        android:background="#1DDB16"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/target"
        android:text="BOTTOM"
        android:id="@+id/bottom"
        android:textSize="20sp"
        android:background="#1DDB16"/>

</RelativeLayout>